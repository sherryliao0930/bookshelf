# 電子書櫃 — 設定說明

一個木頭書架樣式的網站：上傳 PDF 和封面，就變成書架上一本一本的電子書，點下去可以翻閱、跳頁、放大縮小。

跟你之前的「班級相簿」一樣，這是**單檔 HTML + Supabase**，不需要安裝 Node.js，也不用編譯。

---

## 一、先把 Supabase 準備好（只做一次，約 10 分鐘）

### 1. 開一個免費專案
1. 到 <https://supabase.com> 用 Google 帳號註冊登入。
2. 按 **New project**。
   - Name：隨便取，例如 `bookshelf`
   - Database Password：隨便設一組並**自己記著**（之後幾乎用不到）
   - Region：選 **Northeast Asia (Tokyo)** 或 **Southeast Asia (Singapore)**，離台灣近比較快
3. 按 Create，等 1～2 分鐘跑完。

### 2. 建立資料表和檔案空間
1. 左邊選單點 **SQL Editor** → **New query**。
2. 打開這個資料夾裡的 `supabase-schema.sql`，**整份複製貼上**。
3. 按右下角 **Run**。看到 Success 就完成了。

> 最後一行 `alter publication ... add table` 如果報錯說「已經存在」，忽略就好，不影響。

### 2.5 已經在用的人：追加一次更新
如果你之前就跑過 `supabase-schema.sql`、書櫃已經在用了，請**再跑一次** `supabase-migration.sql`（一樣是 SQL Editor 貼上按 Run）。它會加上：
- `position` 欄位 → 拖曳排序才存得住
- 修改權限 → 改書名、換封面才存得進去

沒跑的話，拖曳和改書名會跳提示告訴你要先做這一步。

### 3. 把金鑰填進 config.js
1. 左邊選單點 **Project Settings**（齒輪）→ **API**。
2. 你會看到兩樣東西：
   - **Project URL**：像 `https://abcdefghijk.supabase.co`
   - **Project API keys** 裡的 **anon / public**：很長一串，開頭是 `eyJ...`
3. 用記事本打開這個資料夾裡的 `config.js`，貼進去：

```js
const SUPABASE_URL = 'https://abcdefghijk.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOi....（很長）';
const ADMIN_PASSWORD = '改成你自己的管理密碼';
const BUCKET = 'books';
const SHELF_TITLE = '我的電子書櫃';
```

存檔，完成。

> `anon key` 本來就是設計成公開放在網頁裡的，不是機密。真正不能外流的是 `service_role` key —— 那一組**絕對不要**貼進來。

---

## 二、怎麼打開來用

**不能直接用滑鼠雙擊 index.html**（瀏覽器會擋本地檔案互相載入）。要用小型網頁伺服器：

```bash
python -m http.server 5512 --directory bookshelf
```

跑起來後，瀏覽器打開 <http://localhost:5512>。

（我已經把它加進 `.claude/launch.json`，之後你叫我「開書櫃預覽」我就能直接啟動。）

---

## 三、放到網路上讓別人看

隨便挑一個，都免費：

- **Netlify Drop**（最簡單）：到 <https://app.netlify.com/drop>，把整個 `bookshelf` 資料夾拖進去，馬上得到一個網址。
- **GitHub Pages**：把資料夾推上 GitHub，Settings → Pages 選 main 分支。
- **Cloudflare Pages**：連 GitHub 專案，自動部署。

得到網址後，**任何人點開都能看書、翻頁**，不用登入。

---

## 四、操作說明

### 書架
- 搜尋框可以找書名或作者
- 點任何一本書就開始閱讀，不需要密碼

### 管理（右下角小齒輪 → 輸入管理密碼）
解鎖後上方會出現一條黃色提示列，書封右上角也會冒出兩顆按鈕：

| 想做的事 | 怎麼做 |
|---|---|
| 上傳新書 | 右下角的黃色「＋ 上傳新書」→ 填書名、選 PDF（**封面可以另外上傳 JPG／PNG**；不選的話系統會自動抓 PDF 第一頁當封面） |
| 改書名／換封面 | 書封右上角的**鉛筆**按鈕，可以改書名、作者，也可以換一張封面（換了之後舊封面會自動刪掉） |
| 刪除電子書 | 書封右上角的**垃圾桶**按鈕 |
| 調整書本順序 | 直接**用滑鼠把書拖到想要的位置**（平板手機用手指拖也可以）。放開就會存回雲端，下次打開順序還在 |
| 離開管理模式 | 黃色提示列右邊的「離開管理模式」 |

拖曳時搜尋框要是空的（搜尋狀態下拖曳會排錯，所以擋掉了）。

### 閱讀器
| 動作 | 怎麼做 |
|---|---|
| 翻頁 | 左右箭頭鍵、畫面左右邊緣的箭頭、工具列 ‹ › |
| 跳到第幾頁 | 工具列中間輸入頁數，按 Enter（按 `G` 可快速跳到輸入框） |
| 放大／縮小 | 工具列 ＋ －、鍵盤 `+` `-`、按住 Ctrl 滾滑鼠滾輪 |
| 切換顯示模式 | 點中間的「整頁」按鈕，循環：整頁 → 符合寬度 → 100% |
| 第一頁／最後一頁 | `Home` / `End` |
| 全螢幕 | 工具列 ⛶ 或按 `F` |
| 關閉回書架 | `Esc` 或左上 ✕ |

會自動記住你每本書看到第幾頁，下次打開直接接著看（記在這台電腦的瀏覽器裡）。

---

## 五、幾件要注意的事

### 1. 管理密碼只是「軟性門檔」
`ADMIN_PASSWORD` 是寫在網頁前端的，懂技術的人打開開發者工具就看得到。它擋得住一般路人亂上傳、亂刪書，**但擋不住有心人**。

所以：**不要放不能外流的東西**（有版權的書、含個資的文件）。書櫃是公開的，拿到網址的人都看得到、也都能下載 PDF。

如果之後要做到真正的權限控管，要改成 Supabase Auth 登入 + 嚴格的 RLS 政策（跟家庭記憶繪本那個專案一樣），跟我說我再幫你改。

想讓大家都能上傳？把 `ADMIN_PASSWORD` 設成 `''` 就好。

### 2. 免費方案的容量
Supabase 免費方案大約 **1 GB 儲存空間、5 GB／月流量**。

PDF 很吃空間：
- 純文字的書約 1～5 MB → 可以放兩三百本
- 掃描的、圖多的書可能 20～50 MB → 大概只能放 20～50 本

另外 Supabase 預設**單一檔案上限 50 MB**。要放更大的檔，到後台 Storage → Configuration 把上限調高（免費方案最高可設到 50 MB，再大就要付費方案）。

超過容量的話有兩條路：升級 Supabase 付費方案（約 25 美元／月，8 GB），或用工具先把 PDF 壓縮再上傳。

### 3. 需要網路
翻閱器用的是 pdf.js（從 CDN 載入），開網頁時電腦要能連網。

---

## 六、檔案結構

```
bookshelf/
├── index.html           書架 + 上傳視窗 + 閱讀器的畫面
├── styles.css           木頭書架樣式（含手機版）
├── app.js               主程式：上傳、書架排版、PDF 翻閱
├── config.js            ★ 你要改的只有這個檔
├── supabase-schema.sql  資料庫設定（第一次設定時跑）
├── supabase-migration.sql  已經在用的人要補跑這份（拖曳排序＋修改權限）
├── preview-desktop.html 版面預覽頁（可以直接用瀏覽器打開看）
└── 設定說明.md          就是這份
```

---

## 七、之後可以再加的功能

想加再跟我說：

- **書籤／畫重點**：在書上標記重點段落
- **分類書櫃**：一層放國文、一層放自然
- **全文搜尋**：搜尋書「裡面」的文字，不只搜書名
- **朗讀**：搭配語音把內文念出來
- **真正的登入權限**：改用 Google 登入，區分老師／學生
